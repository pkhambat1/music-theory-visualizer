import { IntervalConnection } from "./IntervalConnection"

/** A chord tone connecting the diatonic row to the mode row (kept through extensions if any). */
export class DiatonicConnection extends IntervalConnection {}
